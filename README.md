# 🌟 [AulaProgramaçãoWebAtividadeIII] - Experiência Prática III: JavaScript Avançado

## 🎯 Objetivo da Atividade

Esta entrega do curso de Análise e Desenvolvimento de Sistemas (ADS) foca em transformar uma interface web estática em uma **Single Page Application (SPA) dinâmica**, utilizando recursos avançados do JavaScript para manipulação do DOM e validação de dados.

## ✨ Funcionalidades Implementadas

O projeto foi desenvolvido para atender aos seguintes requisitos técnicos e funcionais:

| Funcionalidade | Implementação | Status |
| :--- | :--- | :--- |
| **Single Page Application (SPA)** | Implementação de roteamento básico (`router.js`) para carregar diferentes seções (Home, Projetos, Cadastro) sem recarregar a página. | ✅ Concluído |
| **Sistema de Templates JS** | Utilização de *templates* em JavaScript (strings HTML) para renderizar o conteúdo dinamicamente na seção principal da aplicação (`router.js`). | ✅ Concluído |
| **Validação de Formulário** | Sistema de verificação de consistência de dados em tempo real (eventos `blur` e `input`) e na submissão, com aviso claro ao usuário de preenchimento incorreto (`main.js`). | ✅ Concluído |
| **Estrutura Modular** | Código organizado em módulos (`router.js`, `main.js`, `script.js`) para gerenciamento de diferentes responsabilidades (Roteamento, Validação, Funções Auxiliares). | ✅ Concluído |

## 🛠️ Tecnologias Utilizadas

* **HTML5:** Estrutura base da aplicação.
* **CSS3:** Estilização responsiva e variáveis CSS (`style.css`).
* **JavaScript (ES6+):** Lógica de SPA, manipulação do DOM, tratamento de eventos e validação de formulários.

## 🚀 Como Executar o Projeto

Este projeto é totalmente *client-side* (Front-end) e não requer instalação de pacotes (Node.js) ou servidor.

1.  **Clone o Repositório:**
    ```bash
    git clone [Link do Seu Repositório]
    ```
2.  **Abra o Arquivo:**
    * Navegue até a pasta do projeto.
    * Dê um duplo clique no arquivo **`index.html`** para abri-lo diretamente no seu navegador (Chrome, Firefox, etc.).

Alternativamente, você pode usar a extensão "Live Server" do VS Code para executá-lo em um servidor local.

## 📁 Estrutura de Pastas

A estrutura modular do código-fonte é a seguinte: