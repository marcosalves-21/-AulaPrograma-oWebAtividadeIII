# 💻 Experiência Prática II - Desenvolvimento Web (ADS)

## 📌 Tema do Projeto

Organização de Voluntariado para Inclusão Social.

## ✨ Objetivos da Entrega (Foco em CSS3)

Esta entrega concentra-se em estilizar a estrutura HTML da Entrega I, aplicando técnicas modernas de CSS3 para criar uma interface visualmente profissional, responsiva e acessível.

## 📋 Especificações Técnicas Implementadas

O projeto atende a todas as especificações técnicas obrigatórias, demonstrando o uso de:

### 1. Sistema de Design
* **Variáveis CSS Customizadas (`:root`):** Utilizadas para cores, tipografia e espaçamento.
* **Paleta de Cores:** Definida com 8 cores (primárias, secundárias, neutras e de feedback).
* **Tipografia:** Hierarquia com 5 tamanhos de fonte.
* **Sistema de Espaçamento Modular:** Baseado em múltiplos de 8px.

### 2. Leiautes Responsivos
* **CSS Grid:** Utilizado na classe `.main-container` e em seções específicas (`.about-content`) para a estrutura geral e sistema de 12 colunas.
* **Flexbox:** Utilizado na navegação (`<nav>`) e nos componentes internos (`.cta-buttons`, `.impact-cards`) para alinhamento e distribuição de elementos.
* **Breakpoints:** Implementação de 5 breakpoints responsivos para garantir a adaptação em diversos dispositivos (do mobile ultra-pequeno ao desktop grande).

### 3. Componentes de Interface
* **Navegação Mobile:** Menu hambúrguer funcional (requer o `script.js` simples).
* **Cards:** Sistema de cards responsivos (`.card`) implementado na página de projetos.
* **Botões:** Estilização com estados visuais (`:hover`, `:active`).
* **Formulários:** Estilizados com validação visual simples (cores de `border` para `:valid` e `:invalid`).

## 📁 Estrutura de Pastas

A estrutura do projeto está organizada modularmente: